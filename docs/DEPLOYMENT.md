Development and deployment plan

Current status

The local Docker Compose, Prisma, and staff authentication configuration are checked in. GitHub Actions CI passed on main at commit 7ba0eca09a318acf7de90afb158359822facc386 (run 36042057377 (https://github.com/simonjr07/nexaservice1/actions/runs/36042057377)); this documentation branch has not run remotely. CI does not deploy. Local PostgreSQL responded to a direct query, all three checked-in migrations were verified as up to date, all 66 tests passed with database tests enabled, and the production build passed. A local next start HTTP smoke check returned 200 for /, /about, /services, /contact, /admin/login, and /icon.svg; unauthenticated /admin redirected to /admin/login. The public pages included the demo notice and the checked routes emitted CSP and nosniff headers. This was a local check, not a hosted deployment. Docker container health was reported healthy by the human engineer but could not be inspected from this shell. A full browser sign-in/sign-out with a provisioned account remains unverified here. A Neon Free project named nexaservice-demo exists in AWS Europe West 2 (London) with its default production branch; the console showed a six-hour restore history. Its direct endpoint was reached over TLS. The initial, User account-status, and rate-limit migrations were applied with prisma migrate deploy; the migration table records all three as finished, and prisma migrate status reports the database up to date. The signed-in Vercel account shows Hobby. No Vercel project, production administrator, or application deployment has been created.

Local PostgreSQL setup

Prerequisites: Node.js 24 LTS (recorded in .nvmrc), npm, Docker Desktop/Engine with Compose, and an available localhost port (5432 by default). compose.yaml runs only official postgres:17-bookworm, publishes its port on 127.0.0.1, stores data in the postgres_data named volume, and uses pg_isready as a health check. This is a local development database, not production hosting.

Step 1: Copy .env.example to .env (PowerShell: Copy-Item .env.example .env). Edit .env with a new local-only POSTGRES_PASSWORD; keep DATABASE_URL consistent with the user, password, database, and port. URL-encode special password characters. Set NEXTAUTH_SECRET to a long random value (for example, generate one locally with node -e "console.log(require('node:crypto').randomBytes(32).toString('base64url'))") and NEXTAUTH_URL=http://localhost:3000. .env is Git-ignored; do not commit it.

Step 2: Run docker compose up -d db.

Step 3: Run docker compose ps and wait for db to report healthy. For a direct check in PowerShell, run docker compose exec db sh -c 'pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"'.

Step 4: Run npm ci, then npm run db:validate and npm run db:generate. The install and build scripts also generate Prisma Client; validation/generation do not require a live database.

Step 5: Run npm run db:deploy to apply checked-in migrations to a fresh or existing local database without generating new migrations. For a later schema change, use npm run db:migrate -- --name descriptive_change against the development database. Run npx prisma migrate status to confirm migration state.

Step 6: Supply your own local administrator name, email, and password and run the explicit provisioning command (SECURITY.md#development-admin-provisioning) for the first ADMIN. The command requires NODE_ENV=development and does not run automatically. Thereafter an active ADMIN can manage internal accounts at /admin/users; no public registration is available.

Step 7: Run npm run dev. npm runs predev first to regenerate Prisma Client, then starts the unchanged next dev command. Visit /admin/login and verify sign-in, role-appropriate placeholders, sign-out, and that /admin redirects back to login after sign-out. The protected workspace checks the current User row on each request.

Stop the container with docker compose down; this keeps the named volume. Do not use docker compose down -v unless you intend to delete all local database data. Changing POSTGRES_USER, POSTGRES_PASSWORD, or POSTGRES_DB after the volume has been initialized does not update existing PostgreSQL credentials; plan such changes deliberately.

Prisma configuration and commands

Prisma CLI, Client, and PostgreSQL adapter are on the compatible 7.10.0 stable line. At selection time, npm's latest tag pointed to Prisma 8.0.0-rc.15, a release candidate, so this project chose the supported stable line rather than a prerelease. Prisma 7.10 uses prisma7.config.ts for the connection URL and prisma/schema.prisma for the datasource provider and Client generator. The generated Client goes to ignored src/generated/prisma; imports belong in server code only. src/server/db/client.ts creates a single adapter-backed Client and requires DATABASE_URL at runtime. Checked-in SQL migrations cover the initial schema and User account status.

Useful commands: npm run db:validate, npm run db:generate, npm run db:deploy, npm run db:migrate (authoring only), npx prisma migrate status, npm run lint, npm run typecheck, npm test, and npm run build. Prisma 7 does not generate Client automatically after migrate dev; run db:generate after schema changes when using Prisma outside npm run dev. The predev hook regenerates Client at every development startup. Never use a reset command against data that must be retained.

Development data

No seed data or administrator account is included. An explicit development-only administrator command is available after migration; it uses locally supplied values and hashes the password. Future seed data should be clearly fictional, limited to non-sensitive demo content, and reproducible. The approved production administrator CLIs are described below; neither has run against Neon, and an actual production bootstrap still needs separate approval. No public or hard-coded administrator password belongs in source control.

GitHub Actions CI

.github/workflows/ci.yml runs on pull requests targeting main and pushes to main, using Node.js 24 LTS and npm ci from the committed lockfile. The one job starts an isolated postgres:17-bookworm service with a health check and a CI-only database/password on the hosted runner. It generates an ephemeral NEXTAUTH_SECRET without printing it. The job validates and generates Prisma Client, applies only checked-in migrations with npm run db:deploy, checks migration status, runs ESLint and next typegen plus TypeScript, runs all Vitest tests with RUN_DATABASE_TESTS=1, and builds the production bundle. A failure blocks that job; there is no deployment, database reset, or production credential in the workflow. Keep changes on a feature branch, open a pull request to main, review the CI result, and merge under the repository's branch policy. The first main run passed; review a fresh run for every changed branch before release.

The repository's package-lock.json makes npm ci deterministic for the checked-in dependency graph; postinstall and prebuild generate Prisma Client, and CI also runs generation explicitly. next/font currently fetches Google-hosted Geist assets during build, so a network-restricted build can still fail. The CI service and development Compose use the same PostgreSQL major/image but separate databases. Do not point RUN_DATABASE_TESTS=1 at valuable data: most integration tests roll back their transactions, but the rate-limit concurrency test cleans up its own temporary bucket.

Production environment and Vercel preparation

Deploy the repository as one Next.js App Router application using the framework preset, Node.js 24 LTS, npm ci, and npm run build; no application Dockerfile or vercel.json is needed for the current architecture. Verify these settings in Vercel before enabling production. postinstall/prebuild regenerate Prisma Client, and the generated Client is Git-ignored. The runtime needs Node.js (not an Edge-only database runtime) for pg, bcrypt, and Prisma. Static assets remain under public/; Next image handling and Geist font fetching need a live preview check. Do not hardcode a localhost URL in hosted settings.

Configure server-only production variables in the deployment secret store: DATABASE_URL for the hosted database, a unique long random NEXTAUTH_SECRET, and canonical HTTPS NEXTAUTH_URL. Set or verify NODE_ENV=production through the platform; do not use local POSTGRES_ Compose values or development ADMIN inputs in production. VERCEL and VERCEL_ENV are platform-provided, not values to fake in production. NEXTAUTH_SECRET must stay stable across instances and deploys or sessions become invalid. Configure preview credentials and callback URLs separately, and verify the preview auth behavior before inviting testers. Never use NEXT_PUBLIC_ for secrets.

Approved trial choices: Use a standalone Neon Free PostgreSQL project for fictional/demo data. The human engineer requests a Vercel Hobby .vercel.app deployment only as a personal, non-commercial demo, with no real customer enquiries, commercial intake, custom domain, or purchase. The form labels the business fictional and requests fictional details. Vercel's Fair Use Guidelines (https://vercel.com/docs/limits/fair-use-guidelines) define commercial use broadly as any deployment used for financial gain and direct uncertain cases to Support. NexaService's original portfolio purpose includes attracting freelance clients, so Hobby deployment is paused until Vercel confirms in writing that this specific use is permitted. If Vercel does not permit it, evaluate another free provider that allows commercial portfolios; do not activate Pro automatically. Genuine client acquisition, retention, and a stronger backup/recovery target require separate decisions.

The approved Free project is nexaservice-demo in London. Its ignored local connection file contains a TLS-enabled pooled URL (identified by Neon's -pooler host suffix). For the approved migration, the process derived the corresponding direct hostname in memory, set sslmode=verify-full, and passed that direct URL only to the PostgreSQL check and Prisma CLI. The credential value was not changed or printed. A separate read-only SELECT 1 through the saved pooled URL succeeded. The eventual application's server-only DATABASE_URL should use the pooled URL after hosting approval; application behavior on that connection has not been smoke-tested. This distinction is operational, not a second schema datasource. Never paste either URL into source, docs, build logs, or a public CI run. Keep production migration credentials out of the verification-only CI workflow. The app's pool is reused only within one server instance, so check the provider's connection limits and idle wake-up latency during smoke tests. Set the host's Function region near London when a compliant project is created and verify the deployed setting.

Local build guard: Next.js loads .env.production.local during next build and next start, ahead of .env. That initially made an ordinary local production-mode command capable of selecting Neon. The URL file has now been moved, with human authorization, to ignored .env.neon.local, which Next.js does not auto-load. The URL value was not displayed or edited. A production-mode environment-load check confirmed that the localhost DATABASE_URL from .env is selected; an ordinary npm run build then passed and reported only .env as its environment file. Supply the Neon URL only to explicit approved operations; do not rename this file back to a Next.js auto-loaded environment filename.

Neon Free's console currently shows a six-hour restore history; limits may change. Use only disposable fictional data for the trial. A restore test must use a disposable branch or backup, never the live branch. Do not claim managed backup coverage or a tested restore until actually verified. Pool sizing and demo-data cleanup/retention are still to be decided. See Neon's current pricing (https://neon.com/pricing) and connection pooling guidance (https://neon.com/docs/connect/connection-pooling).

Task #13's rate limiter requires the RateLimitBucket table before traffic reaches the new application. Counters live in PostgreSQL and therefore work across local, CI, and serverless instances. On Vercel it trusts the edge-overwritten x-vercel-forwarded-for header; outside Vercel it uses one shared unidentified bucket rather than trusting caller-supplied IP headers. Verify the forwarded-header behavior and add a reviewed edge firewall/rate-limit policy in the actual deployment. If database access fails, login and enquiry fail closed; this is an availability dependency.

Controlled production release sequence — migration complete, deployment pending

Step 1: Review migration SQL and its compatibility with the running app. Use the approved Neon Free project, configure TLS, inspect its restore options, and confirm the database contains only disposable fictional data. A real-enquiry launch would require a separately approved retention and recovery plan.

Step 2: Configure production-only secrets and the canonical HTTPS URL in Vercel; choose Node.js 24 and the approved environment. Keep credentials out of source control and local development files.

Step 3: Completed on Neon: Applied the three checked-in migrations using prisma migrate deploy through the direct endpoint, then verified each finished migration record and an up-to-date prisma migrate status. Do not use migrate dev, db push, or reset on Neon. The verification-only GitHub Actions workflow deliberately does not connect to Neon.

Step 4: The production-only bootstrap CLI is implemented and tested on a disposable local database. The engineer reports that earlier Neon attempts failed with Prisma P2028 and that the Neon ADMIN count remains zero; this change does not run the CLI against Neon. The serializable interactive transaction now allows up to 20 seconds to start and 30 seconds to complete. After migrations and a separate explicit approval for a production run, supply temporary ADMIN_NAME, ADMIN_EMAIL, ADMIN_PASSWORD, DATABASE_URL, and NODE_ENV=production in a restricted release shell, then run npm run admin:bootstrap-production -- --confirm-production-admin-bootstrap. The script enforces the existing staff password policy, hashes with bcrypt cost 12, and refuses if any ADMIN already exists. A successful run creates exactly one ACTIVE ADMIN. It does not run at startup or through HTTP. Remove temporary ADMIN inputs immediately after the run and verify the account through a private login test. Do not put these inputs in Vercel's permanent runtime environment. The development-only commands remain unchanged.

Step 5: A separate recovery CLI is also implemented but has not run against Neon. After a separately approved recovery operation, supply temporary ADMIN_EMAIL, replacement ADMIN_PASSWORD, DATABASE_URL, and NODE_ENV=production, then run npm run admin:recover-production -- --confirm-production-admin-recovery. It refuses a missing, disabled, or non-ADMIN account; in a serializable transaction it updates only passwordHash for the existing ACTIVE ADMIN and verifies bcrypt. It never creates an account or changes profile/status/relationships/timestamps. Remove temporary inputs after use. Do not run recovery as part of normal deployment.

Step 6: Deploy the application only after migration and bootstrap prerequisites are satisfied and the hosting plan permits the intended use. Hobby requires Vercel confirmation for this portfolio case; otherwise use a separately approved compliant plan. Verify /, /services, /contact, /admin/login, and unauthenticated /admin redirect. Confirm the public contact page clearly says it is a fictional demo. Confirm TLS, secure cookies, response/cache headers, published-content visibility, and a rate-limited test without locking a real administrator. With explicit permission, submit one clearly fictional enquiry, verify the corresponding dashboard record and mutations, then decide whether to retain or safely remove it.

Step 7: Observe errors, database connections, rate-limit behavior, and deployment health. A failed application release can be rolled back to a compatible prior build. A database migration is not automatically reversible: use a reviewed forward fix or restore plan based on a tested backup. Prefer backward-compatible migration phases where possible.

Before the fictional demo goes public, resolve the pending lead visibility/status policy, demo-data retention/deletion policy, edge controls, complete CSP, production cache behavior, actual administrator bootstrap approval/execution, and dependency advisories. The database region is approved as London; choose a nearby application-function region when hosting is configured. A later real-enquiry launch also requires an approved backup/restore objective and customer-data policy. Do not treat a green CI job as deployment approval.

Future environments

The CI and controlled-release sections above distinguish verified checks from remaining production operations. Neon Free is provisioned and migrated, but no hosted application uses it; Hobby suitability for this portfolio is unresolved. No application deployment or production ADMIN provisioning has occurred. HSTS is emitted only when VERCEL_ENV=production; verify it over HTTPS on the eventual hosted hostname. A different hosting platform needs a reviewed trusted-client-IP integration.

Known limitations

Docker container health could not be inspected here, though the human engineer reported it healthy. The latest local SELECT 1, Prisma migration status, database-enabled tests, and production build succeeded. The full database-enabled suite also passed on Node.js 24 (66 tests); Prisma Client generation and a guarded build passed on Node.js 24. Real login/provisioning with a durable account were not verified. The public shell still uses Google-hosted Geist fonts at build time; an offline build may fail to fetch them even when the database setup is correct.

On 2026-09-25, a fresh npm audit --omit=dev --json reached the registry and reported four high-severity findings in the Prisma CLI dependency tree: @prisma/config, deepmerge-ts, mysql2, and prisma. The installed chain is Prisma 7.10.0 → @prisma/config 7.10.0 → deepmerge-ts 7.1.5, plus mysql2 3.15.3. The project uses PostgreSQL and a static Prisma config, but these facts do not clear the advisories. npm's automatic fix would downgrade Prisma to 6.19.3; npm's prev stable tag remains 7.10.0 while latest points to an 8.0.0 release candidate. No override or downgrade was applied. Recheck for a compatible stable fix and assess the residual risk before public deployment.

Task #16 removed the 414 old .npm-cache artifacts from Git tracking, and that change is now merged into main. Git reports zero tracked cache paths on main and on the current documentation branch. Ignored local cache files may remain on disk.
